fastapi==0.140.13
starlette==1.3.1
uvicorn[standard]==0.35.0
httpx==0.28.1
pydantic-settings==2.10.1
openai>=2.0.0,<3.0.0
pymongo>=4.12.0,<5.0.0
prometheus-client==0.22.1
# Reads monitoring/alert_rules.yml, so the alerting posture is measured from the file
# Prometheus loads rather than asserted in code. Arrives with uvicorn[standard] too,
# but a direct import earns a direct pin.
PyYAML==6.0.3
